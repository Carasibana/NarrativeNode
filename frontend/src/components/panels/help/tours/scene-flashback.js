// AUTO-GENERATED help tour (Phase 6.1) from the help capture manifest.
// Surface: scene-flashback. Regions are measured from the capture; prose merges from
// help-prose.json (Phase 6.2a) and falls back to placeholder where unauthored.
// The screenshot is referenced by FILENAME and resolved at runtime in
// helpTours.js, so a missing / renamed / retired image shows the placeholder
// instead of breaking the Vite build with an unresolved static import.

export const tour = {
  id: "scene-flashback",
  category: "Concepts",
  tier: "base",
  parent: "scene-node",
  order: 6,
  title: "Scene Flashback",
  intro: "A flashback scene depicts events from an earlier time than the moment at which the reader encounters it. It is anchored to a parent scene and shows that scene's cast and their state as it stood there, so you can revisit an earlier beat without breaking the forward order of the rest of your story. Because it looks back rather than carrying the story onward, a flashback is read-only: it reflects what was already established and adds no new changes of its own.",
  screenshotFile: "main-app.webp",
  screenshotAlt: "Scene Flashback screenshot.",
  sections: [
    { id: "flashback_scene", label: "Flashback marker", region: { x: 78, y: 47.7, w: 5.7, h: 18.2 }, body: "Marks this scene as a flashback, set apart from a scene sitting in straightforward sequence. It signals that the scene revisits an earlier point in the story, drawing its cast and their state from the parent scene it looks back to rather than from the scenes around it on the canvas." },
  ],
}
